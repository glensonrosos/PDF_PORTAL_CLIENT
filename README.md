# Client (React)

## Setup
1. npm install
2. Ensure `.env` has `REACT_APP_API_BASE_URL=http://localhost:5007`
3. npm start (runs on port 3007)

## Routes
- /login
- /dashboard (protected)
- /admin (protected + admin)
  - /admin/users
  - /admin/import
  - /admin/files
